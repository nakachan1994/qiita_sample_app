<% if post.liked_by?(current_user) %>
  <p>
    <%= link_to post_likes_path(post), method: :delete, remote: true, class:'text-danger' do %>
      <i class="fas fa-heart"></i><%= post.likes.count %> いいね
    <% end %>
  </p>
  <% else %>
  <p>
    <%= link_to post_likes_path(post), method: :post, remote: true, class:'text-dark' do %>
      <i class="far fa-heart"></i><%= post.likes.count %> いいね
    <% end %>
  </p>
<% end %>